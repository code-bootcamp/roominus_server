version: '3.7'

services:
    roominus-server:
        platform: linux/x86_64
        image: asia.gcr.io/codecamp-main-project/roominus:1.102
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./src:/Roominus/src
        env_file:
            - ./.env
        ports:
            - 3000:3000

    # my-database:
    #     platform: linux/x86_64
    #     image: mysql:latest
    #     environment:
    #         MYSQL_DATABASE: 'roominus'
    #         MYSQL_ROOT_PASSWORD: 'root'
    #     ports:
    #         - 3306:3306

    # my-redis:
    #     image: redis:latest
    #     ports:
    #         - 6379:6379
